// import { MySqlInstance } from "./MySqlInstance";
// import mysql from 'mysql'

// export abstract class AbstractRepository {

//     private readonly instance: mysql.Connection;

//     constructor(instance: MySqlInstance) {
//         this.instance = instance
//     }

//     protected async save<T>(object: T): Promise<unknown> {
//         await this.instance.
        
//         // const [rows] = await this.bigqueryClient.query({ query, params })

//         // return rows.map(data => extractObject(data, objKlass))
//     }
// }